# Frontend configuration (Vite). Copy to .env to override.
# Base path for API calls. Defaults to "/api" which is proxied to the backend in dev.
VITE_API_BASE=/api
# Dev proxy target for the backend service.
VITE_API_TARGET=http://localhost:4000
